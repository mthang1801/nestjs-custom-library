import {
  getPageSkipLimit,
  recursivelyStripNullValues,
  typeOf,
  valueToBoolean,
} from './function.utils';

const utils = {
	valueToBoolean,
	typeOf,
	getPageSkipLimit,
	recursivelyStripNullValues,
};

export default utils;
