import {
  compareHashedString, getPageSkipLimit,
  hashedString,
  recursivelyStripNullValues,
  typeOf,
  valueToBoolean
} from './function.utils';

const utils = {
	valueToBoolean,
	typeOf,
	getPageSkipLimit,
	recursivelyStripNullValues,
	hashedString,
	compareHashedString,
};

export default utils;
